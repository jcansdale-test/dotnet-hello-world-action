env:
  DOTNET_NOLOGO: true

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - run: dotnet new console

    - run: dotnet publish -c Release

    - run: git status
